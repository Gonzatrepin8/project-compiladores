/*
 * Prueba de declaraciones de variables globales y locales,
 * siguiendo la gramática de TDS25.
 */
program {
    integer global_var = 0;
    bool global_flag = true; 

    void main() {
        integer local_var = 4;
        bool another_flag = false;
        
        // Las asignaciones son sentencias separadas
        global_var = 100;
        global_flag = true;
        local_var = 50;
        another_flag = false;
    }
}
program {
    // Función que retorna un valor
    integer add(integer a, integer b) {
        return a + b;
    }

    // Función void
    void do_nothing() {
        return;
    }

    void another_do_nothing() {
    }

    integer pow(integer base, integer exponent) extern;

    void main() {
        integer result = 0;
        integer pow2 = pow(2, 2);
        result = add(5, 3);
        do_nothing();
        another_do_nothing();
    }
}

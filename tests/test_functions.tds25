program {
    // Función que retorna un valor
    integer add(integer a, integer b) {
        return a + b;
    }

    // Función void
    void do_nothing() {
        return;
    }

    void main() {
        integer result = 0;
        result = add(5, 3);
        do_nothing();
    }
}
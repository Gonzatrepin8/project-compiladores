program {
    void main() {
        integer count = 10;
        
        if (count > 5) then {
            // Rama then
        }

        while count > 0 {
            count = count - 1;
            if (count == 5) then {
                // if anidado
            } else {
                // else anidado
            }
        }
    }
}